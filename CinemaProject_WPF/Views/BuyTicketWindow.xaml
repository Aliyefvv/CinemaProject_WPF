<Window x:Class="CinemaProject_WPF.Views.BuyTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:CinemaProject_WPF.Views" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="BuyTicketWindow" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#363636">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Row="0">
            <DatePicker DisplayDateStart="{Binding Mode=OneWay, Source={x:Static System:DateTime.Today}}" DisplayDateEnd="{Binding DisplayDateEnd}" BorderThickness="0" Width="180" Canvas.Left="20" Canvas.Top="15" SelectedDate="{Binding SelectedDate}"/>

            <Border Canvas.Top="215" Canvas.Right="20" Width="175" Height="110" Background="#D9C590" CornerRadius="10">
                <StackPanel HorizontalAlignment="Left" Margin="5,2,0,0">
                    <Viewbox Height="25" Margin="5">
                        <RadioButton GroupName="g1" IsChecked="{Binding RadioButton1}" FontSize="16" FontWeight="DemiBold" VerticalContentAlignment="Center">13:00-15:00</RadioButton>
                    </Viewbox>
                    <Viewbox Height="25" Margin="5">
                        <RadioButton GroupName="g1" IsChecked="{Binding RadioButton2}" FontSize="16" FontWeight="DemiBold" VerticalContentAlignment="Center">16:00-18:00</RadioButton>
                    </Viewbox>
                    <Viewbox Height="25" Margin="7,5,5,5">
                        <RadioButton GroupName="g1" IsChecked="{Binding RadioButton3}" FontSize="16" FontWeight="DemiBold" VerticalContentAlignment="Center">20:00-22:00</RadioButton>
                    </Viewbox>
                </StackPanel>
            </Border>

            <Label Canvas.Bottom="85" Canvas.Left="20" Style="{StaticResource MovieElements_1_LabelStyle}">Total : </Label>
            <Label Canvas.Bottom="85" Canvas.Left="90" Style="{StaticResource MovieElements_2_LabelStyle}" Content="{Binding Total}"/>
            <Label Canvas.Bottom="85" Canvas.Left="132" Style="{StaticResource MovieElements_2_LabelStyle}" Content="azn"/>

            <Button Canvas.Bottom="20" Canvas.Right="20" Background="#CCA43B" Foreground="Black" Width="180" Height="50" FontSize="22" FontWeight="DemiBold"
                    Content="Buy Ticket !" Command="{Binding BuyTicketCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="7"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Resources>
            </Button>
        </Canvas>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <UniformGrid Grid.Row="0" Columns="6" Rows="2" Margin="0,0,0,5">
                <ToggleButton CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Content}" Background="#E5E5E5" FontSize="18" BorderThickness="0"
                              Margin="17,10" Style="{StaticResource SelectChairButtonStyle}" >1
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">2
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">3
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">4
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">5
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">6
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">7
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">8
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">9
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">10
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">11
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">12
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedExpensiveSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ToggleButton>
            </UniformGrid>

            <UniformGrid Grid.Row="1" Columns="6" Rows="4" Margin="0,5,0,0">
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">13<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">14<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">15<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">16<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">17<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">18<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">19<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">20<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">21<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">22<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">23<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">24<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">25<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">26<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">27<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">28<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">29<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">30<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">31<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">32<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">33<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">34<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">35<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
                <ToggleButton Background="#E5E5E5" FontSize="18" BorderThickness="0" Margin="17,10" Style="{StaticResource SelectChairButtonStyle}">36<i:Interaction.Triggers>
                        <i:EventTrigger EventName="Unchecked">
                            <i:InvokeCommandAction Command="{Binding UnCheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Checked">
                            <i:InvokeCommandAction Command="{Binding CheckedCheapSeatCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers></ToggleButton>
            </UniformGrid>
        </Grid>
    </Grid>
</Window>
